import styled from "@emotion/styled";

export default styled.input`
  font-weight: bold;
  background-color: rgb(var(--secondary-bg));
  color: #ffffff;
  padding: 15px;
  border-radius: 30px;
  border: none;
  outline: none;
`;